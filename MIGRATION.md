# فرع Eleventy الاحتياطي — نسخة بديلة قابلة للنقل من Cloudflare

## هدف الفرع ده
الموقع حاليًا شغّال بحل بسيط وذكي (HTMLRewriter داخل src/index.js) بيوحّد
الفوتر وقت الطلب — وده الحل المستخدم فعليًا على `main` ومفيش داعي تغييره.

الفرع ده (`eleventy-alternative`) نسخة بديلة كاملة باستخدام Eleventy —
محفوظة للاحتياط فقط، في حالة الرغبة يومًا في الانتقال لاستضافة غير
Cloudflare (حيث ميزة HTMLRewriter مش متاحة). **مش للاستخدام الفوري.**

## الحالة
- 46/46 صفحة اتحوّلت من أحدث نسخة من الملفات (شاملة تعديلاتك الأخيرة:
  أزرار المشاركة، تحديثات الفوتر اليدوية).
- تم التحقق: تطابق 100% في عدد الـ`<script>`/`<link>`/`<meta>`/`<img>`
  بين كل صفحة أصلية والناتج المبني.
- قائمة روابط الفوتر (`content/_data/footerNav.js`) متزامنة مع
  `FOOTER_NAV_LINKS` في `src/index.js` — لو عدّلت واحدة يومًا، عدّل التانية.

## لو قررت تستخدمه يومًا
```bash
npm install
npm run build     # ينتج dist/
```
عدّل `wrangler.toml`: `[assets].directory = "./dist"`، واحذف كود
`applyUnifiedFooter`/`buildFooterNavHtml` من `src/index.js` (مش محتاجينه
بعد كده لأن الفوتر بيتبني وقت البناء مش وقت الطلب).
